cmake_minimum_required(VERSION 3.16.0)
project(mit6824)

set(CMAKE_CXX_FLAGS "-std=c++11 -Wall -g -O0")

set(SOURCE_DIR ${ROOT_DIR}/source)
set(INCLUDE_DIR ${ROOT_DIR}/include)
set(MR_SRC_DIR ${SOURCE_DIR}/mr)
set(MR_INC_DIR ${INCLUDE_DIR}/mr)

include_directories(${INCLUDE_DIR})
include_directories(${MR_INC_DIR})
aux_source_directory(${MR_DIR} SRC)

add_executable(mit6824 ${SRC})
install(TARGETS mit6824 DESTINATION ${INSTALL_DIR})